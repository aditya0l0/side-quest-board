{
  "name": "sidequest-webhook-server",
  "version": "1.0.0",
  "description": "GitHub Issues → Jenkins webhook receiver for the SideQuest CI pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
